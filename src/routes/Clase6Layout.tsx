import ClaseLayout from "../components/ClaseLayout";

const Clase6Layout = () => {
  return (
    <ClaseLayout
      claseNumber={6}
      claseTitle="useEffect y data fetching"
    />
  );
};

export default Clase6Layout;
