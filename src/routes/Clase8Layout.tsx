import ClaseLayout from "../components/ClaseLayout";

const Clase8Layout = () => {
  return (
    <ClaseLayout
      claseNumber={8}
      claseTitle="React Router - Rutas, Hooks y Navegación"
    />
  );
};

export default Clase8Layout;
