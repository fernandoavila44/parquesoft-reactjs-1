import ClaseLayout from "../components/ClaseLayout";

const Clase5Layout = () => {
  return (
    <ClaseLayout
      claseNumber={5}
      claseTitle="Prop drilling y useContext"
    />
  );
};

export default Clase5Layout;
