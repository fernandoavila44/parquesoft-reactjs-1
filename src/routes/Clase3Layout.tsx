import ClaseLayout from "../components/ClaseLayout";

const Clase3Layout = () => {
  return (
    <ClaseLayout 
      claseNumber={3} 
      claseTitle="Ciclo de vida y eventos" 
    />
  );
};

export default Clase3Layout;
